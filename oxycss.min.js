document.addEventListener('DOMContentLoaded', () => {
    // root config
    let primary = "rgb(0, 0, 0)";
    let secondary = "rgb(14, 14, 14)";

    // elements
    let body = document.body;
    let div = document.querySelector('div');
    let code = document.querySelector('code');

    let inputs = [document.querySelector('input'), document.querySelector('button'), document.querySelector('textarea')];

    // universal styles
    document.querySelectorAll('*').forEach(el => {
        el.style.fontFamily = "'Courier New', Courier, monospace";
        el.style.color = "white";
    });

    // body reset
    body.style.background = primary
    body.style.margin = "0";
    body.style.padding = "0";

    // style form elements
    inputs.forEach(el => {
        if (el) {
            el.style.background = secondary;
            el.style.border = "solid 5px";
            el.style.borderRadius = "10px";
            el.style.padding = "10px";
            el.style.margin = "10px";
        }
    });

    // style div
    if (div) {
        div.style.background = primary;
        div.style.border = "solid 5px";
        div.style.borderColor = secondary;
        div.style.margin = "10px";
        div.style.padding = "10px";
        div.style.borderRadius = "10px";
        div.style.boxShadow = "0px 0px 5px #fff";
    }
});
